# Environment Variables (copy to .env.local)
# Supabase — optional, app works in demo mode without these
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# OpenRouter — optional, demo mode uses pre-written responses
VITE_OPENROUTER_API_KEY=
